// The brief defines /oak-park as the main store page (canonical).
// The site root mirrors it so visitors landing on / see the same on-brief page.
export { default, metadata } from "./oak-park/page";
